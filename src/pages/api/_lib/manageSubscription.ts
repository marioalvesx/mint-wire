export async function saveSubscription(
  subscriptionId: string,
  customerId: string
) {
  // Busca usuário no banco do FaunaDB com o ID {customerId}
  // Salvar os dados da subscription no FaunaDB
}
